# Cache Strategy

Use cache entries as derived data, never as the system of record. Invalidate or version cache keys during schema or model changes, and define explicit fallback behavior for stale reads.
